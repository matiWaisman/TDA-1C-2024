#include <vector>

using namespace std;

vector<int> catalogo;
vector<vector<int>> matriz;


int () main{
    catalogo = {6, 2, 5, 3};
    matriz = vector<vector<int>>(catalogo.size() + 1, vector<int>(catalogo.size() + 1, -10000));
    return 2;
}